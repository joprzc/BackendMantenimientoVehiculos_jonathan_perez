Actúa como mi asistente técnico para implementar la OPCIÓN A de mi proyecto “Proyecto Tesis DB PostgreSQL”:

OBJETIVO:
Conectar un OBDLink MX+ por Wi-Fi y automatizar la captura de datos OBD-II hacia mi sistema (Django + PostgreSQL). Quiero un “collector” en Python que lea PIDs en tiempo real y los envíe a un endpoint REST de mi backend. El backend guarda en PostgreSQL y luego mi dashboard (AdminLTE + Chart.js) los grafica.

CONTEXTO:
- Backend: Django + Django REST Framework
- DB: PostgreSQL
- Ya tengo tablas como `obd_data` y/o `vehicle_telemetry` con columnas: timestamp, vehicle_code, engine_rpm, vehicle_speed_kph, engine_temp_c, oil_pressure_psi, battery_voltage_v, fuel_level_percent, engine_failure_imminent.
- No quiero procesos manuales (ni CSV). Todo debe ser automático: conectar Wi-Fi al OBDLink y empezar a capturar.

REQUISITOS:
1. Hazlo paso a paso (micro-pasos), sin romper el código.
2. En cada paso dime EXACTAMENTE:
   - Qué archivos debo tocar (ruta y nombre)
   - Qué código agregar/cambiar (bloques completos)
   - Qué librerías instalar (pip) y para qué sirven
   - Cómo configurar variables (IP/puerto del OBDLink, intervalo de lectura)
   - Cómo probar/verificar que el paso quedó bien (comandos, Postman, consultas SQL, logs)
3. Incluye manejo de errores:
   - Si no hay conexión al OBDLink, reintentar sin crashear
   - Si un PID no existe, guardar null
   - Logs claros (con timestamps)
4. Seguridad básica:
   - Proteger el endpoint con token (JWT o Token simple)
   - Validación con serializer (DRF)
5. Entregables:
   - Script `collector/obd_collector.py` (o un comando Django `manage.py collect_obd`)
   - Endpoint `POST /api/obd/ingest/`
   - Serializer + view + urls
   - Consultas SQL para verificar inserts y últimas lecturas
   - Checklist de pruebas

PRIMERA RESPUESTA QUE QUIERO EN ESTE CHAT:
- Un plan general de 6–10 pasos numerados.
- Luego empezar con el PASO 1 (preparación de entorno) y dar instrucciones completas.
- Asume macOS como mi entorno principal, pero dame notas si cambia en Windows.

NOTA:
Si el OBDLink MX+ requiere IP/puerto específico, indícame cómo detectarlo/confirmarlo y cómo probar conectividad (ping/telnet/nc).
